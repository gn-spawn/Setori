extends layout

block content
  h1 セトリシェア
  p セットリストをシェア

  form(method="post", enctype="multipart/form-data", action="/upload")
    input(type="file", name="playlist", accept="application/xml")
    input(type="submit").btn
